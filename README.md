# clinical_ml_pipeline
An end-to-end AI/ML engineering pipeline that trains a clinical readmission risk model, evaluates it for fairness and data drift, and benchmarks an embedding-based document retrieval system — all with full reproducibility guarantees.
